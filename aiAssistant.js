function answer(q){q=q.toLowerCase();if(q.includes('search')&&q.includes('property'))return'Open GIS Map and search a prototype Property ID such as LS-1024 or a location.';if(q.includes('boundary'))return'Use the GIS Map to inspect parcel boundaries. A possible mismatch is only a flag and requires official verification.';if(q.includes('application'))return'Use Applications to track submitted land requests and their current status.';return'I can help navigate LandSphere and explain available platform information. I do not invent legal or official information.'}module.exports={answer};