const admin=require('firebase-admin');async function requireAuth(req,res,next){if(!admin.apps.length)return res.status(503).json({error:'FirebaseNotConfigured'});try{const h=req.headers.authorization||'';if(!h.startsWith('Bearer '))return res.status(401).json({error:'BearerTokenRequired'});req.user=await admin.auth().verifyIdToken(h.slice(7));next()}catch(e){res.status(401).json({error:'InvalidToken'})}}module.exports={requireAuth};